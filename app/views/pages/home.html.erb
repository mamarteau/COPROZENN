<div class="container">
  <h4>Mes prochaines réunions :</h4>
  <div class="row">
    <% @meetings.each do |meeting| %>
      <div class="col-6">
        <div class="card">
          <div class="title-card">
            <p><%= meeting.date %></p>
          </div>
          <p class="card-title"><%= meeting.title %></p>
          <p class="card-text"><em><%= meeting.description %></em></p>
        </div>
      </div>
    <% end %>
  </div>
</div>
